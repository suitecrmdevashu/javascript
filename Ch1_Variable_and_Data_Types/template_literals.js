//Template Literals
//Template Strings use back-ticks (``) rather than the quotes ("") to define a string:

let name = 'Ashu Sharma';
console.log(`My Name is ${name}`);

console.log(`my age is ${30 + 20}`);

let message = `my name is 
Ashu
Sharma`;
console.log(message);