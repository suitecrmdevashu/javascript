//logical Operator
// and (&&), OR (||), not(!)

let price = 20;

console.log(price > 10 && price <25); //Both condition will be true then return true.

console.log(price > 50 || price <= 20); //One of condition will be truen then return true.

console.log(!(price>=20)); // reverese the result from true to false and vice versa